//Notación 

//; --> delimitar el final de una linea
const b = 4;
b+4;

//. --> Se usa en los objetos para acceder a los atributos.
// movil.anchura

// [] --> Se usan para las listas, arreglos o arrays

const ar = [1, 2, 3, 4]
console.log(ar[2])

// () --> Se usan para funciones
function suma(a, b) {
    //Se escribe la función
}

//{} --> Llaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
}

if (true) {
//todo lo que haya entre llaves

const constante2 = "hola"
}